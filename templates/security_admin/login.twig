<!DOCTYPE html>
<html lang="lt">
<head>
    <meta charset="utf-8">
    <title>{% block title %}{{'login:login title'|trans}}{% endblock %}</title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />

    <meta name="robots" content="noindex,nofollow" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <meta name="format-detection" content="telephone=no, date=no" />

    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="sha512-1ycn6IcaQQ40/MKBW2W4Rhis/DbILU74C1vSrLJxCq57o941Ym01SwNsOMqvEBFlcgUa6xLiPY/NS5R+E6ztJQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <link rel="stylesheet" type="text/css" href="{{ asset('/public/admin/css/login.css') }}" media="all" />
</head>

<body class="d-flex align-items-sm-center">

<div class="login-wr container">
    <div class="text-center">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
    </div>

    <div class="login-content bg-light">
        <div class="lg-wr">
            <h1>{{ 'login:form title'|trans }}</h1>
            <form id="login" method="post" autocomplete="off">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                <div class="form-group mb-3">
                    <label for="u" class="mb-2">{{ 'login:username'|trans }}:</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus placeholder="{{ 'login:username'|trans }}">
                </div>
                <div class="form-group mb-3">
                    <label for="p" class="mb-2">{{ 'login:password'|trans }}:</label>
                    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="off" required placeholder="{{ 'login:password'|trans }}">
                </div>
                <button type="submit" class="btn btn-primary">Prisijungti</button>
                <div class="clearfix"></div>
            </form>
        </div>
    </div>
    <div class="login-footer">Alius Sultanovas PKIM</div>
</div>
</body>
</html>
